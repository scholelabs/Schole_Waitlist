generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Waitlist {
  id     Int    @id @default(autoincrement())
  email  String @unique
  school String
  name   String
  phone  String
}
